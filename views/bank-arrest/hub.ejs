<%
const canonical = 'https://zakonexpertt.kz/arest-scheta-v-bankah-kazahstana';
const title = 'Арест счёта в банках Казахстана: все банки и причины | ZakonExpert';
const description = 'Что делать, если арестовали или заблокировали счёт в банке Казахстана. Страницы по 23 банкам, проверка ЧСИ, КГД, суда и финмониторинга.';
const schema = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'CollectionPage',
      '@id': canonical + '#webpage',
      url: canonical,
      name: title,
      description,
      dateModified: reviewedAt,
      inLanguage: 'ru-KZ',
      publisher: { '@type': 'Organization', name: 'ZakonExpert', url: 'https://zakonexpertt.kz' },
      mainEntity: {
        '@type': 'ItemList',
        numberOfItems: pages.length,
        itemListElement: pages.map((item, index) => ({
          '@type': 'ListItem',
          position: index + 1,
          name: item.brand,
          url: 'https://zakonexpertt.kz' + item.path,
        })),
      },
    },
    {
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Главная', item: 'https://zakonexpertt.kz/' },
        { '@type': 'ListItem', position: 2, name: 'Аресты в банках Казахстана', item: canonical },
      ],
    },
  ],
};
const body = include('../partials/bank-arrest-hub-body', { pages, legalPages, reviewedAt });
%>
<%- include('../news/layout', {
  title,
  description,
  canonical,
  ogType: 'website',
  ogImage: '/img/brand/zakonexpert-logo-transparent-hd.png',
  schema,
  noindex: false,
  enableAutoAds: false,
  extraStyles: ['/css/bank-arrest-cluster.css?v=20260823-1'],
  extraScripts: ['/js/growth-pages.js?v=20260823-1'],
  body,
}) %>
