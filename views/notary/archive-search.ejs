<%
const q = String(query || '').trim();
const unpublishedPage = directory && directory.unpublished ? Number(directory.unpublished.page) || 1 : 1;
const pageTitle = 'Замена нотариуса в Казахстане — кому передан архив | ZakonExpert';
const pageDesc = 'Проверка прекратившего деятельность нотариуса, подтверждённых передач архива и записей, где хранитель публично не указан. Ежедневная сверка с ЕНИС.';
const canonical = 'https://zakonexpertt.kz/zamena-notariusa';
const schema = {
  '@context': 'https://schema.org',
  '@type': 'WebApplication',
  name: 'Проверка передачи архива нотариуса',
  description: pageDesc,
  url: canonical,
  applicationCategory: 'LegalService',
  operatingSystem: 'Any',
  isAccessibleForFree: true,
  potentialAction: {
    '@type': 'SearchAction',
    target: canonical + '?q={notary_name}',
    'query-input': 'required name=notary_name',
  },
};
const body = include('../partials/notary-archive-body', { query: q, directory, lastUpdated, chambers });
%>
<%- include('../news/layout', {
  title: pageTitle,
  description: pageDesc,
  canonical,
  ogType: 'website',
  ogImage: null,
  schema,
  noindex: Boolean(q || unpublishedPage > 1),
  extraStyles: ['/css/notary-archive.css?v=20260823-1', '/css/notary-archive-directory.css?v=20260824-1'],
  body,
}) %>